abstract class GenericMenuItem {
  int  id;
  int categoryId;
  String classification;
  // late Image image;
  String description;   

  GenericMenuItem({required this.id, required this.categoryId, required this.classification,  required this.description});
  
  
}